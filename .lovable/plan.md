

## Plan: Update Branding (Title, Favicon, Remove Lovable References)

### Changes

1. **`index.html`**: Update `<title>` to "EnPesos | Más pesos en tu cuenta", update all meta tags (og:title, description, author) to remove Lovable references, update favicon link to point to `/favicon.png` (since the uploaded file is PNG).

2. **Favicon**: Copy the uploaded logo (`user-uploads://Gemini_Generated_Image_v13pmrv13pmrv13p.png`) to `public/favicon.png` and reference it in `index.html`.

3. **Remove Lovable references**: Scan and clean any remaining "Lovable" mentions in `index.html` meta tags (og:image, twitter:image, twitter:site, description, author).

